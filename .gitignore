node_modules/
.next/
out/
peace-and-purpose/

# Local agent/skill tooling — not site source
.agents/
.claude/
.impeccable/review/
.impeccable/mocks/

# Secrets — never commit real credentials
.env
.env.local
.env.*.local
*.key
*.pem

# Backups / stray files
*.bak
*.old
